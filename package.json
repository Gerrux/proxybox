{
  "name": "proxybox-ui",
  "private": true,
  "version": "0.1.0",
  "description": "proxybox — контроль исходящего трафика выбранных приложений, fail-closed",
  "license": "GPL-3.0-or-later",
  "type": "module",
  "packageManager": "pnpm@9.15.9",
  "scripts": {
    "build": "pnpm -r build",
    "lint": "pnpm -r lint",
    "validate": "pnpm lint && pnpm build && cargo test --workspace"
  }
}
